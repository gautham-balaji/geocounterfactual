# GeoCounterfactual backend - core runtime (Phases 1-3, 5)
# Python 3.12 required. Heavy generator deps live in requirements-generator.txt
# so Phase 1-3 work does not pull ~2 GB of torch.

# ---- API layer ----
fastapi>=0.115,<1
uvicorn[standard]>=0.30
sse-starlette>=2.1
pydantic>=2.8,<3
pydantic-settings>=2.4
python-dotenv>=1.0

# ---- Agent orchestration ----
# Spec pinned langgraph 0.2.20 / langchain-core 0.3.0; both are ~2 years stale
# and langchain-core 0.3 cannot satisfy langgraph 1.x. Tracking the 1.x line.
langgraph>=1.0,<2
langchain-core>=1.0,<2

# ---- Planner LLM (Gemini) ----
langchain-google-genai>=4.0,<5

# ---- Earth observation ----
# Spec pinned earthengine-api 0.1.415; the 1.x line is current and changes the
# init contract (ee.Initialize(credentials=..., project=...)).
earthengine-api>=1.7,<2
requests>=2.32

# ---- Raster / numerics ----
# numpy held below 2.0 for torch 2.4 compatibility in Phase 4.
numpy>=1.26,<2
rasterio>=1.4,<2
scikit-image>=0.24
scipy>=1.14
pillow>=10.4
matplotlib>=3.9

# ---- Tests ----
pytest>=8.3
